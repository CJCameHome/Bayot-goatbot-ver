# Set the language
language = "javascript"

# Install dependencies when Repl boots
onBoot = "npm install"

# Define the command to start the bot
run = "node index.js"

# (Optional) Configure database if needed
# Example for sqlite:
# REPLIT_DB_URL = "file:./data.db" 

# (Optional) Configure database if needed
# Example for mongodb:
# REPLIT_DB_URL = "mongodb://localhost:27017/your-database-name"